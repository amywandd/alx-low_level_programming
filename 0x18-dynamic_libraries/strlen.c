#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: parameter function
 * Return: 0 (success)
 */
int _strlen(char *s)
{
int len = strlen(s);
int i = 0;
while (i < len)
{
i++;
}
return (i);
}
