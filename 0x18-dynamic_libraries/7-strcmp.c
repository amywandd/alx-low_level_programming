#include "main.h"
#include <string.h>
/**
 * _strcmp - compares two strings
 * @s1: parameter function
 * @s2: parameter function
 * Return: 0 (success)
 */
int _strcmp(char *s1, char *s2)
{
int n;
n = strcmp(s1, s2);
return (n);
}
