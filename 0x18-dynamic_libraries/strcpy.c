#include "main.h"
#include <string.h>
/**
 * *_strcpy - copies strings
 * @dest: parameter function
 * @src: parameter function
 * Return: 0 (success)
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
