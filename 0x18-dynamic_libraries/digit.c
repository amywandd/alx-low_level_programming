#include "main.h"
/**
 * _isdigit - checks whether c is a digit
 * c: parameter function
 * Return: 1 is a digit, 0 otherwise
 */
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
