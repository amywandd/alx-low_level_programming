#include <stdio.h>
/**
 * sub - function that subtracts two numbers
 * @a: parameter function
 * @b: parameter function
 * Return: subtractio of two numbers
 */
int sub(int a, int b)
{
return (a - b);
}
