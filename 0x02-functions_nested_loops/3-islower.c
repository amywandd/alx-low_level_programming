#include "main.h"
/**
 * c - parameter function.
 * _islower - checks whether c is lowercase
 * Return: 1 if lowercase, 0 if otherwise
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
